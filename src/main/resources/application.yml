server:
  port: ${PORT:8080}
  servlet:
    context-path: ${BASE_URL:/}

springdoc:
  api-docs:
    enabled: true
    path: /v3/api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui.html

spring:
  application:
    name: game-service

  data:
    mongodb:
      #uri: ${DB_URL:mongodb://localhost:27017}ee
      uri: mongodb+srv://dodoworkshop:9AvL6YRxytDEJRDZ@dodoworkshop.ere0eew.mongodb.net/game_service_db?retryWrites=true&w=majority&appName=DodoWorkshop

      auto-index-creation: true
